# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Node
node_modules/

# Build output (generated by `npm run build`)
assets/js/
assets/css/
assets/media/
_data/manifest.json

# Ruby
vendor/
.bundle/
# Not committed: the Gemfile resolves differently on Ruby 2.x vs 3.x (see the
# version guard there), so a single lockfile cannot serve both. CI pins Ruby 3.3.
Gemfile.lock

# OS / editor
.DS_Store
*.swp
